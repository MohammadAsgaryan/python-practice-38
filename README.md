# Employee Sorter  
Sort employees by height and weight using Python

## 📌 Project Description  
This project is a simple Python program that reads employee information from a text-based database file.  
Each employee has **Height**, **Weight**, and **Name** fields.

After reading the data, the program sorts employees based on:

1. **Height** → Descending (tallest to shortest)  
2. If two employees have the same height:  
   **Weight** → Ascending (lighter first)


